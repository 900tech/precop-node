name: 🛡️ PRECOP Sentinel Artifacts Forge

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  forge_artifacts:
    name: 🔨 Build Universal Bastion (${{ matrix.os }} / ${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact_name: precop-node-linux
            asset_name: precop-node-linux
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: precop-node-macos
            asset_name: precop-node-macos
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: precop-node-win.exe
            asset_name: precop-node-win.exe

    steps:
      - name: 🛰️ Clone Gateway
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ Install Toolchain (STRICT 1.81.0)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.81.0
          targets: ${{ matrix.target }}

      - name: 🛡️ Install Dependencies (Linux Jammy)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake clang llvm libssl-dev build-essential perl

      - name: 🛡️ Install Dependencies (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          choco install nasm cmake strawberryperl
          echo "C:\Program Files\NASM" >> $GITHUB_PATH

      - name: 🔍 Pre-Forge Audit (Universal Shell)
        shell: bash
        working-directory: ./engine
        run: |
          echo "🔍 Checking Root..."
          ls -la
          echo "🔍 Checking Doc/RPC..."
          ls -la doc/rpc/ || echo "🚨 doc/rpc MISSING"

      - name: 🔨 Forge Sovereign Engine (JAMMY COMPAT)
        shell: bash
        working-directory: ./engine
        env:
          CC: clang
          CXX: clang++
          RUST_BACKTRACE: 1
        run: |
          # 🛡️ FORGE COMPATIBLE GLIBC 2.35 (JAMMY)
          cargo build --verbose --release --target ${{ matrix.target }} -p florestad -j 2
          
      - name: 📦 Prepare Binary (Universal Pack)
        shell: bash
        run: |
          EXT=""
          [[ "${{ matrix.os }}" == "windows-latest" ]] && EXT=".exe"
          
          mkdir -p bin/
          cp engine/target/${{ matrix.target }}/release/florestad${EXT} bin/${{ matrix.asset_name }}

      - name: 📦 Seal Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: bin/${{ matrix.asset_name }}
          if-no-files-found: error

  release:
    name: 👸 Final Scellement - Sovereign Release
    needs: forge_artifacts
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Clone Gateway
        uses: actions/checkout@v4

      - name: 📥 Collect Artifacts
        uses: actions/download-artifact@v4
        with:
          path: bin/
          merge-multiple: true

      - name: 🚀 Launch v1.0.3 Sentinel Release
        uses: softprops/action-gh-release@v2
        with:
          # 🏹 FILTRAGE CRITIQUE : Uniquement les binaires, pas de dossiers ou fichiers cachés.
          files: |
            bin/precop-node-linux
            bin/precop-node-macos
            bin/precop-node-win.exe
          tag_name: ${{ github.ref_name }}
          name: 🛡️ PRECOP Sentinel v1.0.3 - Sovereign Production Release
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
